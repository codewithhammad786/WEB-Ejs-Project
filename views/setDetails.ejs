<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= set.name %> - LEGO Set Details</title>
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <%- include('partials/navbar', { page: '/lego/sets' }) %>

  <div class="container mx-auto mt-10">
    <div class="hero bg-base-200 rounded-lg p-6">
      <div class="hero-content text-center">
        <div class="max-w-md">
          <h1 class="text-5xl font-bold"><%= set.name %></h1>
          <p class="py-6">Below, you will find detailed information about the set: <%= set.name %></p>
        </div>
      </div>
    </div>
    
    <div class="flex flex-col md:flex-row bg-base-100 shadow-xl rounded-lg overflow-hidden">
      <div class="w-full md:w-1/2">
        <img class="object-cover w-full h-full" src="<%= set.img_url %>" alt="<%= set.name %>" />
      </div>
      <div class="w-full md:w-1/2 p-6">
        <h2 class="text-2xl font-bold mb-4"><%= set.name %></h2>
        <p class="mb-2"><strong>Theme:</strong> <a href="/lego/sets?theme=<%= set.theme.toLowerCase() %>" class="btn btn-theme"><%= set.theme %></a></p>
        <p class="mb-2"><strong>Year Released:</strong> <%= set.year %></p>
        <p class="mb-2"><strong>Number of Parts:</strong> <%= set.num_parts %></p>
        <p class="mb-4"><strong>Description:</strong> <%= set.description %></p>
        <div class="card-actions justify-end">
          <a href="/" class="btn btn-primary">Return</a>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
